# Заняття 1 — Вступ до Node.js

## Node.js

- Що таке Node.js
- Виконання JS поза браузером
- Ініціалізація проєкту з `npm init`
  - Властивість `type: module`
  - `nodemon`
- Глобальні об'єкти
  - `process`
  - `import.meta`
- Вбудовані модулі Node.js
  - `node:fs`
  - `node:path`

## Архітектура клієнт-сервер

- REST API — що це і навіщо
- Запити та відповіді
- HTTP-методи: `GET`, `POST`, `PUT`, `DELETE`
- Статуси відповідей: `200`, `201`, `400`, `404`, `500`
- Заголовки запиту та відповіді
- Шляхи та ресурси
- Параметри запиту (`query params`)
- Демонстрація через Postman

## Стартові файли у проєкті

- `assets/cars.json` — масив даних автомобілів для демонстрації роботи з файловою системою

## Послідовність заняття

1. Показати що Node.js це JS поза браузером — запустити простий скрипт через `node index.js`
2. Ініціалізувати проєкт через `npm init` — пояснити `type: module` та підключити `nodemon`
3. Розібрати глобальні об'єкти — `process.env`, `process.argv`, `import.meta.url`
4. Показати `node:path` — побудова шляхів до файлів
5. Показати `node:fs` — читання `assets/cars.json`
6. Пояснити архітектуру клієнт-сервер — HTTP-методи, статуси, заголовки
7. Демонстрація через Postman — показати запити та відповіді
